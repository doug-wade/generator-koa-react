language: node_js
node_js:
  - v5

install:
  - npm install

script:
  - npm run lint
  - gulp build
  - npm test

after_success:
    - './node_modules/.bin/nyc report --reporter=text-lcov | ./node_modules/.bin/coveralls'
